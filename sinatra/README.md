librosa for bpm analysis (uploading a drum loop and detecting" bpm)

"basic pitch" (spotify) for pitch detection and converstion into midi

pyFluidSynth (python wrapper for FluidSynth) fluidsynth basically takes a midi and outputs it in the desired instrument
